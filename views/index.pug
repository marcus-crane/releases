extends layout.pug

block title
    title Upcoming Releases

block content
    include partials/header.pug
    include mixins/game.pug
    div#wrapper
        each val in games
            +game(val.name, val.date, val.bgcover)
    include partials/footer.pug
